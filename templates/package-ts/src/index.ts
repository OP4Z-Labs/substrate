/**
 * {{NAME}} — public entry point.
 *
 * Replace this stub with the actual exports for your package.
 */
export const NAME = "{{NAME}}";

export function hello(): string {
  return `hello from {{NAME}}`;
}
