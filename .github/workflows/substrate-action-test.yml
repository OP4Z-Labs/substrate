name: substrate-action-test

# Dogfood the substrate GitHub Action against the substrate repo itself.
# Runs `substrate doctor` and `substrate audit --list` via the local
# `./action.yml` entrypoint to prove the action surface works end-to-end.
#
# Why a dedicated workflow (not part of ci.yml): the action installs
# substrate via npx, which fetches from npm. For pre-release substrate
# work (when the version isn't published yet), this workflow can be
# pointed at a local-install via the action's `version` input or
# temporarily disabled. ci.yml stays clean of npm-fetch dependencies.
on:
  push:
    branches: [main]
  pull_request:
    paths:
      - "action.yml"
      - "dist/action/**"
      - ".github/workflows/substrate-action-test.yml"

jobs:
  doctor:
    name: substrate doctor via action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # Use the action from the same checkout (./ refers to the repo root).
      # Once published, downstream consumers will write `uses: BeauGoldberg/@op4z/substrate@v0.8.0`.
      - name: Run substrate doctor via action
        id: doctor
        uses: ./
        with:
          command: "doctor --json"
          # Substrate isn't usable from this repo without a `substrate init` first;
          # most action tests should run in a freshly-initialized fixture dir.
          # For this dogfood run we accept that doctor will warn loudly — the
          # acceptance signal is "the action wraps the CLI end-to-end."
          fail-on: "none"

      - name: Show captured output
        env:
          EXIT_CODE: ${{ steps.doctor.outputs.exit-code }}
          REPORT_PATH: ${{ steps.doctor.outputs.report-path }}
        run: |
          echo "Exit code: $EXIT_CODE"
          echo "Report at: $REPORT_PATH"

  audit-list:
    name: substrate audit --list via action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Bootstrap a substrate project for the action to run inside
        run: |
          mkdir -p fixture
          cd fixture
          # Install substrate locally to invoke init (the action will use npx
          # in the next step, but init must run before audit --list has
          # anything to enumerate).
          npm init -y > /dev/null
          npm install ../packages/substrate > /dev/null
          npx substrate init --name action-fixture --short-code AF --bridge claude --quiet

      - name: Run substrate audit --list via action
        id: audit
        uses: ./
        with:
          command: "audit --list"
          working-directory: ./fixture
          fail-on: "error"

      - name: Verify a non-empty audit list was returned
        run: |
          # The action writes substrate-report.json into the working-directory.
          test -f ./fixture/substrate-report.json
          # The stdout output should mention at least one audit (e.g. backend).
          grep -E "(backend|frontend|pre-merge)" ./fixture/substrate-report.json
