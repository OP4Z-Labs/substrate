{
  "name": "@cadence/adapter-stub",
  "version": "0.5.0",
  "description": "Reference TaskAdapter for cadence — prints every call rather than touching a tracker. Useful for testing the plugin contract and as a copy-paste starting point for real adapters.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "private": true
}
