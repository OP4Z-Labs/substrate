# {{NAME}}

TypeScript package scaffolded by Substrate.

## Scripts

```bash
npm install
npm run build      # tsc -b
npm test           # vitest run
npm run lint       # eslint
npm run typecheck  # tsc --noEmit
```

## Layout

```
src/        Source code
tests/      Vitest specs
dist/       Build output (gitignored)
```
